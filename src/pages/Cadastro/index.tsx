import BarraLatertal from '../../containers/BarraLateral'
import Formulario from '../../containers/Formulario'

const Cadastro = () => (
  <>
    <BarraLatertal mostrarFiltros={false} />
    <Formulario />
  </>
)

export default Cadastro
